import React from 'react';
import ReactDOM from 'react-dom/client';
import TruckLoadingPrototype from './TruckLoadingPrototype.jsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TruckLoadingPrototype />
  </React.StrictMode>
);